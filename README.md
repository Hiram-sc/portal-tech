# Tech News

Este projeto é uma página HTML simples que organiza notícias de tecnologia de forma semântica e acessível.

## Descrição

A página traz três seções de interesse:  

- **🖥️ Navegadores mais rápidos**: comparação entre os principais navegadores, com dicas de atualização.  
- **🛠️ Novo framework lançado**: informações sobre um framework recente adotado por grandes empresas.  
- **💡 Dicas de tecnologia**: área lateral com sugestões rápidas relacionadas aos artigos.

Cada seção contém um título, parágrafo explicativo e botão “Ver mais” para aprofundamento.

## ♿ Acessibilidade

O site foi desenvolvido com atenção à acessibilidade, incluindo:

- Estrutura semântica com `<header>`, `<main>`, `<article>`, `<aside>` e `<footer>`.
- Atributos ARIA como `role="banner"`, `aria-label` e `aria-labelledby`.
- Títulos hierárquicos (`<h1>`, `<h2>`, `<h3>`) para organizar o conteúdo.